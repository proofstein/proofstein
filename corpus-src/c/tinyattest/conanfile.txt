[requires]
openssl/3.5.5
# Pulled in for the mesh transport prototype in the `wip/mesh` branch. Nothing
# on main links against it yet.
libsodium/1.0.20 #@PS c-dep-libsodium|libsodium|5|algorithm|declared crypto dependency (provides XSalsa20-Poly1305, Ed25519); nothing on main links against it

[generators]
CMakeDeps
CMakeToolchain

[options]
openssl/*:shared=True
